---
name: dekc-capture-table
description: Capture a table with columns, layer, schema into DEKC/OKF.
---

Run the **dekc-capture-table** skill.

User request: `$ARGUMENTS`

Follow `${CLAUDE_PLUGIN_ROOT}/skills/dekc-capture-table/SKILL.md` completely. Prefer deterministic helpers under `${CLAUDE_PLUGIN_ROOT}/scripts/`. Write valid OKF Markdown with absolute links. DEKC depends on PKC + OKF.

---
name: dekc-semantic
description: Capture semantic models, metrics, dashboards, reports.
---

Run the **dekc-semantic** skill.

User request: `$ARGUMENTS`

Follow `${CLAUDE_PLUGIN_ROOT}/skills/dekc-semantic/SKILL.md` completely. Prefer deterministic helpers under `${CLAUDE_PLUGIN_ROOT}/scripts/`. Write valid OKF Markdown with absolute links. DEKC depends on PKC + OKF.

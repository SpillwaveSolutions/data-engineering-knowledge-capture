---
name: dekc-init
description: Scaffold a DEKC knowledge bundle with medallion catalogs.
---

Run the **dekc-init** skill.

User request: `$ARGUMENTS`

Follow `${CLAUDE_PLUGIN_ROOT}/skills/dekc-init/SKILL.md` completely. Prefer deterministic helpers under `${CLAUDE_PLUGIN_ROOT}/scripts/`. Write valid OKF Markdown with absolute links. DEKC depends on PKC + OKF.

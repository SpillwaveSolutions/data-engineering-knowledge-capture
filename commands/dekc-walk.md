---
name: dekc-walk
description: Walk a data lake/warehouse filesystem and capture tables, SQL, DAX, parquet datasets.
---

Run the **dekc-walk** skill.

User request: `$ARGUMENTS`

Follow `${CLAUDE_PLUGIN_ROOT}/skills/dekc-walk/SKILL.md` completely. Prefer deterministic helpers under `${CLAUDE_PLUGIN_ROOT}/scripts/`. Write valid OKF Markdown with absolute links. DEKC depends on PKC + OKF.

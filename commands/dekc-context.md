---
name: dekc-context
description: Build progressive-disclosure context packs for a data concept.
---

Run the **dekc-context** skill.

User request: `$ARGUMENTS`

Follow `${CLAUDE_PLUGIN_ROOT}/skills/dekc-context/SKILL.md` completely. Prefer deterministic helpers under `${CLAUDE_PLUGIN_ROOT}/scripts/`. Write valid OKF Markdown with absolute links. DEKC depends on PKC + OKF.

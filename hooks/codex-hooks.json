{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/scripts/dekc-curate.sh\"",
            "timeout": 45
          }
        ]
      }
    ]
  },
  "description": "Codex port of Claude Code hooks. Same scripts; ExitPlanMode omitted (no such tool)."
}
